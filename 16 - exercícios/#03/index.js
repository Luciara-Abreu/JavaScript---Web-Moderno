/**
 * 03) Crie uma função que recebe dois parâmetros, base e expoente, e retorne a
 * base elevada ao expoente.
 */

const elevadoAPoencia = (x, exp) => {
  console.log(resultado = x ** exp);
};

console.log(elevadoAPoencia(3, 3));
